모델링이 주다.
BM-DEVS는 모델링을 하는 방법이지 rule을 만드는 방법이 아님.
모델을 먼저 그리고 그 뒤에 rule을 만들어야 함.
연구하는 방법이 simulation modeling을 통해서 하는 것임
simulation model 대상을 옮기고 rule은 model 안에 있는 것
model안에서 rule을 처리하는 것이기 때문에 
TL을 통해 똑같은걸 처리하더라도 기능을 주겠다는 의미.

모델을 먼저!! 이게 최우선으로 그리고 
rule이 어느 모델에 있는가? => BM-DEVS에서는 coupled model에 있음 

sensor node가 왜 atomic model이여야 하는가?
기능 2개를 처리 하는 것. 2개의 다른 기능을 처리 하는 것 그걸 하나의 atomic model로 표현하는 것은 어려움
PQ 모델을 P와 Q로 따로 처리 해서 하듯이.
구조는 논리적 구조도 있고 물리적 구조도 있음
coupled model diagram으로 먼저 그리고 rule을 정하면 쉽게 
atomic model을 coupling model로 표현한 부분을 class note에서 찾아서 할 것!